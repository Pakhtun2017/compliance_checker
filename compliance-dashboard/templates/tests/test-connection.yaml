apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compliance-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "compliance-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compliance-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
